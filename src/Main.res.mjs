// Generated by ReScript, PLEASE EDIT WITH CARE

import * as UI from "./UI.res.mjs";
import * as Html from "./Html.res.mjs";
import * as Util from "./Util.res.mjs";
import * as State from "./State.res.mjs";

var refRoot = {
  contents: undefined
};

function render(state, root) {
  root([
        "",
        ""
      ], [{
          NAME: "Element",
          VAL: UI.page(UI.header(["Rescript + uhtml"], []), UI.content([
                    "<div class=\"",
                    "\" onclick=",
                    ">\n         Hello, ",
                    ", clicked: ",
                    " times.\n         ",
                    "\n        </div>"
                  ], [
                    {
                      NAME: "Str",
                      VAL: Html.css(["\n                    padding: 2em 1em;\n                    border: 0.3em dashed silver;\n                    box-shadow: 1px 1px 3px steelblue;\n                    margin: 0 1em;"], [])
                    },
                    {
                      NAME: "Procedure",
                      VAL: onClicked
                    },
                    {
                      NAME: "Str",
                      VAL: state.name
                    },
                    {
                      NAME: "Int",
                      VAL: state.iterations
                    },
                    {
                      NAME: "Elements",
                      VAL: [
                        UI.testingAllPossibleValues,
                        UI.cursedLogo
                      ]
                    }
                  ]), UI.footer)
        }]);
}

function dispatch(action) {
  var root = Util.unref(refRoot);
  var state = State.updateState(Util.unref(State.mutatedState), action);
  if (root !== undefined) {
    render(state, root);
  }
  
}

function onClicked() {
  dispatch(State.Action.ofType("Update"));
}

function main() {
  var renderer = Html.getRendererFor(document.getElementById("root"));
  refRoot.contents = renderer;
  render(State.initialState, renderer);
}

window.onload=main
;

var unref = Util.unref;

var cx = Util.cx;

export {
  unref ,
  cx ,
  refRoot ,
  render ,
  dispatch ,
  onClicked ,
  main ,
}
/*  Not a pure module */
